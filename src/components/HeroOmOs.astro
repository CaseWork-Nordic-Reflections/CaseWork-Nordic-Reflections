<div class="hero">
  <img
    src="public/Images/hjemmeside_billeder/Hammerknuden__Nordic_Reflections_Copyright_st-scaled.webp"
    alt="Hero billede"
    class="hero-image"
  />
  <div class="hero-text">
    <h1>"Små Pauser med hver sin historie og hvert sit billede"</h1>
  </div>
</div>

<style>
  /* Generel styling for hero sektionen */
  .hero {
    position: relative;
    width: 100%;
    max-width: 1440px;
    height: 60vh;
    margin: 0 auto;
    overflow: hidden;
  }

  /* Billedet */
  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Tekstlag over billedet */
  .hero-text {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #fff;
    text-align: center;
    z-index: 10;
    background: rgba(0, 0, 0, 0.5);
    padding: 1rem 2rem;
    border-radius: 8px;
    font-size: 1.5rem;
    line-height: 1.4;
  }

  .hero-text h1 {
    font-size: clamp(1.5rem, 5vw, 2.5rem line-height: 1.4;);
    line-height: 1.2;
    margin: 0;
  }

  /* Responsiv justering til mindre skærme */
  @media (max-width: 880px) {
    .hero {
      height: 40vh;
    }
    .hero-text {
      font-size: 1rem;

      position: absolute;

      transform: translate(-50%, -50%);
      color: #fff;
      text-align: center;
      z-index: 10;
      background: rgba(0, 0, 0, 0.5);
      padding: 0rem;
      border-radius: 8px;
      line-height: 1.4;
    }
  }

  /* Responsiv justering til mindre skærme */
  @media (max-width: 768px) {
    .hero {
      height: 40vh;
    }
    .hero-text {
      font-size: 1rem;

      position: absolute;

      transform: translate(-50%, -50%);
      color: #fff;
      text-align: center;
      z-index: 10;
      background: rgba(0, 0, 0, 0.5);
      padding: 0rem;
      border-radius: 8px;
      line-height: 1.4;
    }
  }

  @media (max-width: 467px) {
    .hero {
      height: 40vh;
    }
    .hero-text {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      color: #fff;
      text-align: center;
      z-index: 10;
      background: rgba(0, 0, 0, 0.7); /* Halvgennemsigtig sort baggrund */
      border-radius: 8px; /* Runde hjørner */
      display: inline-block; /* Baggrunden tilpasses teksten */
      max-width: 80%; /* Bredden begrænses for pænt layout */
    }

    .hero-text h1 {
      font-size: 1.7rem;
      line-height: 1.2;
      margin: 0;
    }
  }

  @media (max-width: 280px) {
    .hero-text h1 {
      font-size: 1.5rem;
      line-height: 1.2;
      margin: 0;
    }
  }
</style>
